# Development Dockerfile with HMR support
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Copy source files
COPY . .

# Expose ports for Vite dev server and Express API
EXPOSE 5173 3001

# Start development servers with HMR
CMD ["npm", "run", "dev"]

